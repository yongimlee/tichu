# Loaded by `vite build --mode mobile` (the Capacitor mobile build).
# The mobile app is NOT same-origin with the server, so it needs the absolute
# URL of the deployed game server to open its Socket.IO connection.
VITE_SERVER_URL=https://tichu-ghv0.onrender.com
